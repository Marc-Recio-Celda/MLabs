# CLAUDE.md

> **This file holds no fact.** Claude Code reads `CLAUDE.md`; every other agent reads
> `AGENTS.md`. Rather than keep two bindings that can disagree, this one imports the one that
> governs — so `AGENTS.md` stays the single source and this is a pointer, not a copy.

@AGENTS.md
